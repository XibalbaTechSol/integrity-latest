# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Real env values (see .env.example) — never commit
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# design-sync: staged converter scripts + build output are regenerated by
# re-running the sync (see .design-sync/NOTES.md), never committed. The
# fork symlink is recreated per clone. config.json/NOTES.md/conventions.md/
# previews/overrides ARE committed (the durable set).
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
